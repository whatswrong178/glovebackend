================================================================
MediGlove Supply v8.8 (Test-Driven Zero-Cost Edition) - 医疗耗材 B2B 系统全局架构、业务流向与权限矩阵
================================================================

## 顶层技术架构体系 (Zero-Cost Cloud-Native Infrastructure)
- **部署层**: Vercel (前端全自动托管) + GitHub (私有代码库)，实现零成本极速 CI/CD 发布。
- **前端极速引擎**: Refine.dev (中后台 CRUD 框架) + React Query (数据缓存与静默更新) + TailwindCSS。
- **数据与风控底座**: Supabase (PostgreSQL)。
  - **高并发风控**: 采用 PostgreSQL 行级悲观锁 (`SELECT FOR UPDATE`)，彻底解决并发撞号问题。
  - **后端零代码化**: 核心结算引擎、公海掉落等逻辑，全部下沉至 `PostgreSQL Triggers` 和 `RPC` 存储过程执行。
  - **无死角鉴权**: 全面启用 `Row Level Security (RLS)`。无需在中间件写权限校验，数据库底层原生隔离数据视野。
- **全局输出层 (Print Engine)**: 全系统单据原生支持一键无边距打印与预览。
- **零成本通讯网关层 (Enterprise Email Routing)**: 发信端绑定专属公司域名，利用 Resend API 动态路由分发（基于模块自动切换 `finance@`, `info@`, `care@` 等身份）；收信端接入 Zoho Mail 免费版单账号多别名机制。
- **全自动测试防御网 (Test-Driven Pipeline)**: 
  - 强制引入 Vitest，针对佣金引擎、拔河分润等财务逻辑实施 100% 覆盖率的 Unit Tests。
  - GitHub Actions 拦截哨兵：任何代码推送必须跑通测试，失败则强制拦截 Vercel 发布，杜绝带 Bug 上线。
- **全局安全防线 (Data Security)**: 禁用右键/截屏键；全局覆盖低透明度动态水印；切屏/录屏瞬间全屏高斯模糊防窥。

---

## 全局系统布局与模块拆解 (System Layout & Features)

### 1. Home (仪表盘与首页)
- **1.1 精简视图矩阵**: 保留核心模块：`Overview`、`Monthly`、`Staff`、`Client`。
- **1.2 动态佣金双轨制**: 分为 `Est. Comm` (预计佣金) 与 `Actual Comm` (实际入账佣金，严格基于货款到账)。
- **1.3 动态晋升进度条**: 智能识别阶段差额，拒绝展示满级目标。
- **1.4 竞赛排行榜与狼性文化**: 展示 Top 总业绩排行榜及拓客王者排行榜。

### 2. Playbook (营销物料与知识库)
- **2.1 极速上传与自动分类**: Admin 拖拽上传物料，通过二级菜单极速打标签。
- **2.2 高利润营销库**: 赋能代理商，结构化存储高毛利产品卖点、场景化营销剧本与短视频素材。

### 3. Product Catalogue (产品目录与供应链映射)
- **3.1 AI 一键极速建档**: Admin 上传报价单，调用完全免费的 Google Gemini 1.5 Flash 视觉接口，全自动精准提取产品名、SKU 与绝密净成本。
- **3.2 供应商强绑定 (Supplier Mapping)**: 新产品强制绑定供货商，支撑智能采购流。
- **3.3 全域价格防线**: 设定绝密净成本 (`costPrice`)、开单底价红线 (`minSellingPrice`) 与指导价 (`suggestedPrice`)。

### 4. Clients (CRM 客户资源管理)
- **4.1 客户创建与权属溯源**: 强制勾选区域（西马/东马）。常驻 `Created By` 锁定开发归属。
- **4.2 拿样系统 (Request Sample)**: 独立生成 Sample DO，不产生 Invoice，财务独立核算营销损耗。
- **4.3 账期信用评级 (Credit Terms Profile)**: 客户档案强制录入账期属性（如：Cash Term, 30 Days, 60 Days），作为自动催款引擎的底层依据。
- **4.4 数据防盗隔离**: 移除业务端导出权，客户修改必须走 `Request to Edit` 审批流。
- **4.5 客户档案动态怠慢指数追踪器 (Neglect Index)**: 在 Client 表中新增 'Neglect_Index' (Int: 0-6)。监控该客户的连续代开单人身份以触发指数变动，支撑拔河引擎。
- **4.6 公海孤儿机制与极速夺权 (Public Pool & First-Blood Claim)**:
  - 员工离职（状态变为 Inactive）瞬间，Supabase Trigger 自动将其名下所有客户标记为 `Is_Orphan = True`，掉入全员可见的公海池。
  - **首单夺权锁**：任何在职 Sales 对公海客户成功开单，系统强制将该客户 Owner 过户给开单人，取消 Orphan 状态，100% 独占当次提成。

### 5. Invoice, Auto-DO & Auto-PO (三单联动与风控中枢)
- **5.1 视图与账期隔离**: 拆分 `Active` 与 `Completed`。
- **5.2 防并发撞号机制**: 单据采用 `YYMMDD-XXXX` 格式，Supabase 事务锁定防撞号。
- **5.3 智能促销规则引擎**: 单笔总量 >= 3 箱起送；区域为西马且总量 >= 5 箱自动免除运费。
- **5.4 动态分润与客户归属权抢夺引擎 (The Tug-of-War Model)**:
  - 中台代开单或非 Owner 开单，触发拔河博弈：提成按怠慢指数呈现阶梯衰减 (1=50/50, 2=40/60, 3=30/70, 4=20/80, 5=10/90)。
  - 当指数满 6，触发【主权易主】协议，客户 Owner 永久变更为代开单人。
  - 若 Owner 亲自补单可执行“服务债赎回”（Index -1），按当前劣势比例分润，逐步夺回利润大头。
- **5.5 终极佣金引擎数学模型 (含高管风控)**:
  1. *净毛利熔断*: `GP = Math.max(0, ((Selling Price - Cost Price) * Qty) - 均摊折扣)`。
  2. *基础提成*: `(GP_A * 20%) + (GP_B * 15%)`。
  3. *KAM 老客津贴追加*: 开单时校验合作期 >= 180天，强制追加 A类+5% 或 B类+3% 的维护提成。
  4. *长效新客悬赏*: 首单达 3 箱发 RM 50；90天满 RM1k发 RM50；180天满 RM2k发 RM100；365天满 RM6k发 RM200。
  5. *阶梯大奖*: A类产品占比 >= 70% 匹配 Ladder 大奖。
  6. **Leader 活跃度对赌与特批豁免权**:
     - **标准红线**：Leader 个人当月业绩必须 >= **RM 50,000**，方可提取直属团队 1% 业绩。
     - **特批豁免**：由 Admin 独家审批，最低保底线可降至 **RM 35,000**。
     - **生死底线**：连续两个月低于底线，全面强制冻结其管理分红与裂变奖池特权。
  7. *裂变伯乐奖*: 成功孵化新 Leader 后，原 Leader 永久提取新团队的 0.5%。

### 6. Delivery Order (交货与物流网关)
- **6.1 DO 双轨制列表**: 强制拆分为 `Invoice DO` (正常交易发货单) 与 `Sample DO` (样品发货单)。两者互不干扰。
- **6.2 电子 POD 闭环**: 现场交割调用平板手写签名 (Base64) + 实拍货物照片。签收后系统自动发 E-DO (PDF) 给客户。

### 7. HR (中台枢纽：复合权限与动态组织)
- **7.1 复合权限矩阵**: 基础角色叠加模块化权限开关（如：允许执行物流送货），实现一号通办。
- **7.2 精细化编制**: 配置真实的 `Department` 和 `Job Title`。
- **7.3 团队裂变 (Spin-off)**: 累计业绩达 RM 50,000 申请晋升 Leader，激活 0.5% 永久伯乐奖。
- **7.4 荣誉里程碑与自动关怀**: 依靠 Make.com 每天扫描入职/生日节点，自动通过 `care@xx.com` 推送专属祝贺邮件。

### 8. Reports (财务大盘与应收账款风控)
- **8.1 Make.com 自动月结**: 每月 1 号凌晨 00:01，利用 Make.com 触发 Supabase RPC，固化不可篡改的佣金与流水 JSON 快照，锁定历史账目。
- **8.2 现金流风控 (AR Aging & 见款发佣)**: 实时追踪账期。业务员提成强锁于 `Est. Comm`。财务确认款项到账、Invoice 转为 `Paid` 后，方可解冻派发，绝对禁止垫资。
- **8.3 账期全自动催款引擎 (Make.com Auto-Dunning System)**: 
  - 结合 Make.com 定时任务，每天自动扫描处于 `Active` 状态的 Invoice 及其客户 `Credit Terms`。
  - **自动触发节点**：在账单到期前 7 天、到期当天、以及逾期后（如 T+3, T+7），系统全自动向客户负责人邮箱发送带有公司官方口吻的专业催款邮件（附件包含 PDF），实现“系统唱黑脸，业务唱白脸”。
- **8.4 交互式组织架构表**: 树状图全景展示裂变网络，节点实时标示 Leader 的 50k/35k 保底达标状态。
- **8.5 供应链预测沙盘**: 按供应商生成历史采购额与热销 SKU 预测折线图。

### 9. Setting (超级管理员配置中心)
- **9.1 全局参数控制台**: 动态管控佣金率、长效悬赏、Leader 标准门槛 (50k)、绝对保底门槛 (35k)、分润比等。
- **9.2 供应商管线**: 统筹管理供应商字典与联络邮箱，保障 Auto-PO 准确投递。
- **9.3 专属企业路由分配器 (Email Routing Setup)**:
  - Admin 可视化配置各业务模块的发件身份。
  - 财务模块 -> `finance@xx.com` ; 运营模块 -> `info@xx.com` ; 人事模块 -> `care@xx.com`
- **9.4 动态邮件模板库**: 内置 Invoice、E-DO、Receipt、PO、以及 Dunning Reminder (催款预警) 的 HTML 代码编辑器，支持调用客户名称、逾期天数等系统变量。

---

## 终极五大角色全岗位权限与风控矩阵 (RBAC Blueprint)

### 角色 1：Admin (超级管理员 / 老板)
**定位**：系统的上帝视角、规则制定者与终极风控官。
- **数据视野**：100% 全局可见。
- **核心权限**：[促销与悬赏总控] 修改起送门槛、免邮区域与新客破冰金额。[知识库中控] 唯一拥有 Playbook 的 Upload/Edit 权限。唯一导出权（CSV）、唯一彻底删除权。

### 角色 2：HR & Finance (人事与财务中台)
**定位**：公司的运转枢纽、资金、物料与数据的“守门员”。
- **数据视野**：全公司订单、客户、人员数据可见，但不可见绝密进货底价和系统参数。
- **核心权限**：[营销损耗审批 - 重点防线] 审核 Sample DO。[悬赏与促销风控] 每月结账核对首单悬赏与免运费订单防刷单。核对流水后将 Invoice 改为 Paid；审批 Auto-PO 发给工厂。

### 角色 3：Leader (团队领袖)
**定位**：带兵打仗的将军。
- **数据视野**：仅限【本人】+【直属团队成员】的数据。
- **核心权限**：[拿样申请权] 发起 Request Sample。[话术与物料调用] 调阅 Playbook 高级营销短视频和剧本。
- **限制**：无上传物料权；无法干预下属的 3 箱起送门槛拦截。

### 角色 4：Sales (一线业务员)
**定位**：前线冲锋陷阵的士兵。
- **数据视野**：100% 孤岛模式。仅限【本人】名下客户、生成的单据及公海客户。
- **核心权限与束缚**：
  - **[强制促销拦截]** 点击 Create Invoice 时，若总量不足 3 箱直接报错；西马满 5 箱自动抹除运费。无篡改权限。
  - **[公海与拔河抢单]** 参与代开单分润博弈，或在公海池抢夺首单触发 Owner 过户。
  - **[首单追踪器]** 追踪首单满 3 箱的 RM 50 悬赏进度。
- **限制**：看不到同事客户；无资料修改权（走审批）。

### 角色 5：Logistics (物流送货员)
**定位**：完成闭环最后一公里的物理执行者。
- **数据视野**：极度受限。仅能看见分配给自己的 Active DO。强制屏蔽所有单价、总价和提成信息。
- **核心权限**：[双轨作业] 面板清晰拆分 Invoice DO 和 Sample DO。[签收闭环] 提交电子签名与拍照，触发 E-DO。

================================================================